# Workflow

- When the user says "ship it" or otherwise signals approval/satisfaction with the current work ("looks good", "nice", "I'm happy with that", etc.), treat it as authorization to commit AND push the pending changes to the remote. Stage, commit, and `git push` without asking again.
