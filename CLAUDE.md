# Workflow

- When the user says "ship it" or otherwise signals approval/satisfaction with the current work ("looks good", "nice", "I'm happy with that", etc.), treat it as authorization to commit the pending changes. Stage and create a commit without asking again.
